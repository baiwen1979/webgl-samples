/* * Copyright 2017, BaiWenJiang. * All rights reserved. *//** @module debug-utils **/(function(root, factory) { // eslint-disable-line    if (typeof define === 'function' && define.amd) {        // AMD. 注册为匿名模块        define([], function() {            return factory.call(root);        });    } else {        // 注册为浏览器全局对象        root.mathUtils = factory.call(root);    }}(this, function(){    "use strict";    var topWindow = this;    /**     * 在控制台输出错误信息     * @param msg {string} 要输出的错误信息     * @param color {string} 错误信息的颜色     */    function error(msg, color) {        color = color || "red";        if (topWindow.console) {            if (topWindow.console.error) {                topWindow.console.error(msg);            } else if (topWindow.console.log) {                topWindow.console.log("%c%s", "color:" + color, msg);            }        }    }    return {        error: error,    };}));