/* * Copyright 2017, BaiWenJiang. * All rights reserved. *//** @module math-utils **/(function(root, factory) { // eslint-disable-line    if (typeof define === 'function' && define.amd) {        // AMD. Register as an anonymous module.        define([], function() {            return factory.call(root);        });    } else {        // Browser globals        root.mathUtils = factory.call(root);    }}(this, function(){    "use strict";    /**     * 返回 min 和 max 之间的随机数     * @param min {number|float} 最小值     * @param max {number|float} 最大值     * @return {number|float} 随机数     */    function rand(min, max) {        if (max === undefined) {            max = min;            min = 0;        }        return min + Math.random() * (max - min);    }    /**     * 返回制定范围内(0~range)的随机整数     * @param range {number|integer} 范围     * @return {number|integer} 随机整数     */    function randInt(range) {        return Math.floor(Math.random() * range);    }    return {        rand: rand,        randInt: randInt,    };}));